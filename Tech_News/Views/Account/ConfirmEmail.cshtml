@{
    ViewBag.Title = "Confirm Email";
}

<h2>@ViewBag.Title.</h2>
<div>
    @if (!ViewBag.confirm)
    {
        <p>Hãy xác nhận email mà chúng tôi đã gửi!</p>
    }
    else
    {
        <p>
            Cảm ơn bạn đã đăng ký ,  @Html.ActionLink("Ấn vào đây để đăng nhập ", "Login", "Account", new { returnUrl = Url.Action("Index", "Home") })
        </p>
    }

</div>
